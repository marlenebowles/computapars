import React, { Fragment } from 'react';
import { Link } from './index';

export default {
	title: 'Components | Link',
};

export const link = () => (
	<Fragment>
		<Link primary>Linked Item</Link>
	</Fragment>
);
